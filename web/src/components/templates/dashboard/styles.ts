import clsx from "clsx";

export const className = {
  container: "mx-5 mt-5 md:mx-10 md:mt-10",
  navItem: clsx(
    "h-10 w-10 border border-slate-200 rounded-full p-2",
    "hover:bg-primary-100 hover:border-primary-100 transition-all cursor-pointer"
  )
};
